# Terraform state & local artifacts
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.*
crash.log
*.tfvars
!example.tfvars

# Generated SSH private key — never commit
*.pem

# local apply logs
apply.log
destroy.log
